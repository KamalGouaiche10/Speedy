<nav class="navbar navbar-expand-lg navbar-dark sticky-top shadow-sm">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="/">
            <span class="fw-bold">Speedy</span>
        </a>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-lg-center">
                <li class="nav-item">
                    <a class="nav-link" href="/restaurants">
                        </i> Ristoranti
                    </a>
                </li>
                
                <% if (!isAuthenticated) { %>
                    <!-- Utente NON loggato -->
                    <li class="nav-item">
                        <a class="nav-link" href="/login">
                         Login
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-outline-light btn-sm ms-lg-2" href="/register">
                            Registrati
                        </a>
                    </li>
                <% } else { %>
                    <!-- Utente LOGGATO -->
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="bi bi-person-circle fs-5 me-1"></i>
                            <span><%= user.name %></span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <!-- Per CLIENTI -->
                            <% if (user.role === 'customer') { %>
                                <li>
                                    <a class="dropdown-item" href="/orders">
                                        <i class="bi bi-bag-check me-2"></i> I miei ordini
                                    </a>
                                </li>
                            <% } %>
                            
                            <!-- Per RISTORATORI -->
                            <% if (user.role === 'restaurant') { %>
                                <li>
                                    <a class="dropdown-item" href="/dashboard">
                                        <i class="bi bi-speedometer2 me-2"></i> Dashboard
                                    </a>
                                </li>
                            <% } %>
                            
                            <!-- Per ADMIN -->
                            <% if (user.role === 'admin') { %>
                                <li>
                                    <a class="dropdown-item" href="/admin">
                                        <i class="bi bi-shield-check me-2"></i> Pannello Admin
                                    </a>
                                </li>
                            <% } %>
                            
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <form action="/api/auth/logout" method="POST" class="form-inline">
                                    <button type="submit" class="dropdown-item text-danger">
                                        <i class="bi bi-box-arrow-right me-2"></i> Logout
                                    </button>
                                </form>
                            </li>
                        </ul>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>